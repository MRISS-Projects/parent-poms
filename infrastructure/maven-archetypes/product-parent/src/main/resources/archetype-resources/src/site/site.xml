<project name="${project.name}">
	<version position="right" />
	<publishDate position="right" format="yyyy-MM-dd - hh:mm:ss" />
	<bannerLeft>
		<name>${artifactId}</name>
		<src>${logo.image}</src>
		<href>${main.server}/site/${release.type}/products/${artifactId}</href>
	</bannerLeft>
	<skin>
		<groupId>${skin.groupId}</groupId>
		<artifactId>${skin.artifactId}</artifactId>
		<version>${skin.version}</version>
	</skin>
	<body>
<!--		<links>
			<item name="Maven" href="http://maven.apache.org" />
		</links> -->
		<menu ref="parent" />
		<menu ref="modules" />
	    <menu name="Documentation">
	    	<item name="Releases History" href="releases-history.html" />
	    </menu>			  		
		<menu ref="reports" />
	</body>
</project>