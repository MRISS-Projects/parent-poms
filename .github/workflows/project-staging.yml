name: Project Staging

on:
  workflow_call:
    inputs:
      git_project:
        type: string
        required: true
        description: 'Target repository name, e.g. dsh'
      branch_name:
        type: string
        required: true
        description: 'RC branch name, e.g. staging-0.3.0-SNAPSHOT-RC'
      appengine_project_version:
        type: string
        required: false
        default: ''
        description: 'GCP App Engine version suffix (empty = skip flag)'
      cloudrun_project_version:
        type: string
        required: false
        default: ''
        description: 'GCP Cloud Run version suffix (empty = skip flag)'
      build_number:
        type: string
        required: false
        default: ''
        description: 'Build number; resolved to RC<n> in step'
      # #78: these three are the last consumer-specific inputs here. They no longer
      # configure the build -- #76 moved that to maven_properties -- and survive only as
      # arguments to the Mongo user-creation step below, which #78 removes along with
      # them and with the service containers.
      mongo_user:
        type: string
        required: false
        default: ''
        description: 'MongoDB application user to create for tests (empty = skip user creation)'
      mongo_password:
        type: string
        required: false
        default: ''
        description: 'MongoDB application user password for tests'
      mongo_database:
        type: string
        required: false
        default: ''
        description: 'MongoDB database to create the application user against'
      maven_properties:
        type: string
        required: false
        default: ''
        description: >-
          Build properties for this project, one `name=value` per line, rendered into
          the <properties> of the github-packages profile in the generated
          settings.xml. Empty leaves that file exactly as it is written.
    secrets:
      DEPLOY_TOKEN:
        required: true
      GCLOUD_PROJECT_ID:
        required: false

jobs:
  staging:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    # Some products' test suites need a live MongoDB/RabbitMQ connection (e.g. dsh-rest-api).
    # These services are idle/unused for products whose tests don't reference them.
    services:
      mongodb:
        image: mongo:6
        ports:
          - 27017:27017
      rabbitmq:
        image: rabbitmq:3-management
        ports:
          - 5672:5672
          - 15672:15672
    env:
      DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: MRISS-Projects/${{ inputs.git_project }}
          ref: ${{ inputs.branch_name }}
          fetch-depth: 0
          token: ${{ secrets.DEPLOY_TOKEN }}

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Set up Maven 3.9.9
        uses: stCarolas/setup-maven@v5
        with:
          maven-version: '3.9.9'

      - name: Configure Maven settings
        run: |
          mkdir -p ~/.m2
          cat > ~/.m2/settings.xml << 'SETTINGS_EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
                    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                    xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                                        https://maven.apache.org/xsd/settings-1.0.0.xsd">
              <servers>
                  <server>
                      <id>releases</id>
                      <username>${env.GITHUB_ACTOR}</username>
                      <password>${env.DEPLOY_TOKEN}</password>
                  </server>
                  <server>
                      <id>MRISS-Projects-maven-repo</id>
                      <username>${env.GITHUB_ACTOR}</username>
                      <password>${env.DEPLOY_TOKEN}</password>
                  </server>
                  <server>
                      <id>MRISS-Projects-maven-repo-plugins</id>
                      <username>${env.GITHUB_ACTOR}</username>
                      <password>${env.DEPLOY_TOKEN}</password>
                  </server>
                  <server>
                      <id>github.com</id>
                      <username>${env.GITHUB_ACTOR}</username>
                      <password>${env.DEPLOY_TOKEN}</password>
                  </server>
                  <server>
                      <id>github</id>
                      <username>${env.GITHUB_ACTOR}</username>
                      <password>${env.DEPLOY_TOKEN}</password>
                  </server>
              </servers>
              <profiles>
                  <profile>
                      <id>github-packages</id>
                      <properties>
                          <github.personal.token>${env.DEPLOY_TOKEN}</github.personal.token>
                          <!-- MAVEN_PROPERTIES -->
                      </properties>
                      <repositories>
                          <repository>
                              <id>MRISS-Projects-maven-repo</id>
                              <url>https://maven.pkg.github.com/MRISS-Projects/maven-repo</url>
                              <releases><enabled>true</enabled></releases>
                              <snapshots><enabled>true</enabled></snapshots>
                          </repository>
                      </repositories>
                      <pluginRepositories>
                          <pluginRepository>
                              <id>MRISS-Projects-maven-repo-plugins</id>
                              <url>https://maven.pkg.github.com/MRISS-Projects/maven-repo</url>
                              <releases><enabled>true</enabled></releases>
                              <snapshots><enabled>true</enabled></snapshots>
                          </pluginRepository>
                      </pluginRepositories>
                  </profile>
              </profiles>
              <activeProfiles>
                  <activeProfile>github-packages</activeProfile>
              </activeProfiles>
          </settings>
          SETTINGS_EOF

      # #76: the heredoc above is quoted, so it cannot interpolate. The action replaces
      # the marker line inside <properties> with the consumer's own build properties,
      # and deletes it when there are none.
      - name: Render consumer Maven properties
        uses: MRISS-Projects/parent-poms/.github/actions/maven-properties@master
        with:
          properties: ${{ inputs.maven_properties }}

      - name: Configure Git
        run: |
          git config --global user.name  "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Create MongoDB user and database
        if: ${{ inputs.mongo_user != '' }}
        run: |
          docker run --rm --network host mongo:6 mongosh --host localhost --eval '
            db.getSiblingDB("${{ inputs.mongo_database }}").createUser({
              user: "${{ inputs.mongo_user }}",
              pwd: "${{ inputs.mongo_password }}",
              roles: [{ role: "readWrite", db: "${{ inputs.mongo_database }}" }]
            });
          '

      - name: Build and Deploy Staging Artifacts
        run: |
          BUILD_NUM="${{ inputs.build_number }}"
          BUILD_NUM="${BUILD_NUM:-${GITHUB_RUN_NUMBER}}"

          GCP_FLAGS=""
          if [ -n "${{ inputs.appengine_project_version }}" ]; then
            GCP_FLAGS="$GCP_FLAGS -Dappengine.project.version=${{ inputs.appengine_project_version }}"
          fi
          if [ -n "${{ inputs.cloudrun_project_version }}" ]; then
            GCP_FLAGS="$GCP_FLAGS -Dcloudrun.project.version=${{ inputs.cloudrun_project_version }}"
          fi

          set +e
          output=$(mvn -B -U \
            -Ddeployment \
            -DintegrationTests \
            -Drelease.type=rcs \
            -Dbuild.number=RC${BUILD_NUM} \
            $GCP_FLAGS \
            clean deploy 2>&1)
          rc=$?
          echo "$output"
          if [ $rc -ne 0 ]; then
            if echo "$output" | grep -qE "status code: 409|HTTP 409|Conflict \(409\)"; then
              echo "WARNING: Artifact already exists (409 Conflict). Continuing."
            else
              exit $rc
            fi
          fi
          set -e

      # #71: an early copy of the guard that also runs at commit time. The build above has
      # already regenerated README.md, so an unresolved placeholder is detectable here —
      # before the site is published. This matters because generate-list-of-issues is
      # configured failOnError=false (root pom.xml), so a GitHub API hiccup, an expired token
      # or a missing milestone leaves ${issues.text.list} literal without failing the build.
      # Without this step the run would go red only at the commit, after artifacts AND the
      # site were published, with no way to finish. Failing here leaves only the artifact
      # deploy done, which is re-runnable (the 409 handling above makes it idempotent).
      - name: Verify the generated README before publishing
        uses: MRISS-Projects/parent-poms/.github/actions/commit-readme@master
        with:
          branch: ${{ inputs.branch_name }}
          verify-only: 'true'

      - name: Deploy Staging Site
        run: |
          BUILD_NUM="${{ inputs.build_number }}"
          BUILD_NUM="${BUILD_NUM:-${GITHUB_RUN_NUMBER}}"

          mvn -B -U \
            -Ddeployment \
            -Drelease.type=rcs \
            -Dbuild.number=RC${BUILD_NUM} \
            -Dsite.deployment.personal.main=file:///tmp/sites \
            site-deploy

      # #71: the README is generated by the inherited readme-generation profile but
      # committed here, once, rather than by an scm:checkin inside process-resources that a
      # forked lifecycle replayed four times. The workspace was checked out with a token, so
      # credentials are persisted and pushing to origin works.
      - name: Commit generated README.md
        uses: MRISS-Projects/parent-poms/.github/actions/commit-readme@master
        with:
          branch: ${{ inputs.branch_name }}
