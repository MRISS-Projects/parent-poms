---
description: Full reactor build (compile + unit tests, install to local repo)
---

Run the following command in the repo root and report the result, summarizing any test failures or
build errors:

```bash
mvn -B -U clean install
```
